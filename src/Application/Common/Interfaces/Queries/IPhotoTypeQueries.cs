using Domain.Models.ToolPhotos;
using LanguageExt;

namespace Application.Common.Interfaces.Queries
{/*
    public interface IPhotoTypeQueries
    {
        Task<IReadOnlyList<PhotoType>> GetAllAsync(CancellationToken cancellationToken);
        Task<Option<PhotoType>> GetByIdAsync(PhotoTypeId photoTypeId, CancellationToken cancellationToken);
        Task<Option<PhotoType>> GetByTitleAsync(string name, CancellationToken cancellationToken);
    }*/
}
